---
title: 'generate'
description: 'Generate code based on `@wagmi/cli` configuration'
---

# `generate`

Generates code based on configuration, using `contracts` and `plugins`.

## Usage

```bash
wagmi generate
```

## Options

| Option                | Description                                                           |
| :-------------------- | :-------------------------------------------------------------------- |
| `-c, --config <path>` | `[string]` path to config file                                        |
| `-r, --root <path>`   | `[string]` root path to resolve config from                           |
| `-w, --watch`         | `[boolean]` watch for changes (for plugins that support `watch` mode) |
