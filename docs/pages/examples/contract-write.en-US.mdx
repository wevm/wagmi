---
title: 'Contract Write'
description: 'Learn how to perform a contract write with wagmi.'
---

import { ContractWrite } from '../../components/examples'

# Contract Write

The following example teaches you how to perform a contract write in wagmi. The example below builds on the [Connect Wallet Example](/examples/connect-wallet) and uses the [`usePrepareContractWrite`](/docs/hooks/usePrepareContractWrite) & [`useContractWrite`](/docs/hooks/useContractWrite) hooks. Try it out before moving on.

<ContractWrite />
