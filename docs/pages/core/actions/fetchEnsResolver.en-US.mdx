---
title: 'fetchEnsResolver'
description: 'Action for fetching resolver for ENS name.'
---

# fetchEnsResolver

Action for fetching the resolver for ENS name.

This is a wrapper around viem's [`getEnsResolver`](https://viem.sh/docs/ens/actions/getEnsResolver.html).

```ts
import { fetchEnsResolver } from '@wagmi/core'
```

## Usage

```ts
import { fetchEnsResolver } from '@wagmi/core'

const resolver = await fetchEnsResolver({
  name: 'awkweb.eth',
})
```

## Return Value

```ts
EnsResolver
```

## Configuration

### name

ENS name to fetch resolver for.

```ts {5}
import { fetchEnsResolver } from '@wagmi/core'

const resolver = await fetchEnsResolver({
  name: 'moxey.eth',
})
```

### chainId (optional)

Force a specific chain id for the request. The wagmi `Client`'s `publicClient` must be set up as a [chain-aware function](/core/client#publicclient-optional) for this to work correctly.

```ts {6}
import { fetchEnsResolver } from '@wagmi/core'

const resolver = await fetchEnsResolver({
  name: 'awkweb.eth',
  chainId: 1,
})
```
