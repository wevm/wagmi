{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts", "src/**/*.test-d.ts"],
  "compilerOptions": {
    "baseUrl": ".",
    "sourceMap": true,
    "paths": {
      // TODO: Needed for `src/connector.ts` to build
      "abitype": ["node_modules/abitype/dist/types"]
    }
  }
}
