{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts", "src/**/*.test-d.ts"],
  "compilerOptions": {
    "noImplicitReturns": false, // React.useEffect doesn't play well with this
    "types": ["node"],
    "lib": ["DOM"]
  }
}
