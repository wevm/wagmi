diff --git a/dist/core/ExecutionError.d.ts b/dist/core/ExecutionError.d.ts
index 3bab3f1..d1ee637 100644
--- a/dist/core/ExecutionError.d.ts
+++ b/dist/core/ExecutionError.d.ts
@@ -1,7 +1,7 @@
 import { type DecodeErrorResultReturnType, type Hex } from 'viem';
 import { Abis } from 'viem/tempo';
 import type { OneOf, UnionOmit } from '../internal/types.js';
-type AllAbis = typeof Abis.abis;
+type AllAbis = (typeof Abis)[keyof typeof Abis];
 type AbiErrorName = Extract<AllAbis[number], {
     type: 'error';
 }>['name'];
diff --git a/dist/core/ExecutionError.js b/dist/core/ExecutionError.js
index c0c5d47..f815af8 100644
--- a/dist/core/ExecutionError.js
+++ b/dist/core/ExecutionError.js
@@ -1,5 +1,6 @@
 import { decodeErrorResult } from 'viem';
 import { Abis } from 'viem/tempo';
+const abis = Object.values(Abis).flat();
 /** Human-readable messages keyed by ABI error name. */
 export const messages = {
     AddressAlreadyHasValidator: 'This address already has a validator.',
@@ -116,7 +117,7 @@ export function parse(error) {
     const data = extractRevertData(error);
     if (data) {
         try {
-            const decoded = decodeErrorResult({ abi: Abis.abis, data });
+            const decoded = decodeErrorResult({ abi: abis, data });
             const template = messages[decoded.errorName];
             return {
                 ...decoded,
diff --git a/src/core/ExecutionError.ts b/src/core/ExecutionError.ts
index edc586f..b4516e4 100644
--- a/src/core/ExecutionError.ts
+++ b/src/core/ExecutionError.ts
@@ -3,7 +3,8 @@ import { Abis } from 'viem/tempo'
 
 import type { OneOf, UnionOmit } from '../internal/types.js'
 
-type AllAbis = typeof Abis.abis
+const abis = Object.values(Abis).flat() as AllAbis
+type AllAbis = (typeof Abis)[keyof typeof Abis]
 type AbiErrorName = Extract<AllAbis[number], { type: 'error' }>['name']
 
 /** Decoded execution error from a Tempo precompile revert. */
@@ -149,7 +150,7 @@ export function parse(error: Error): ExecutionError {
   const data = extractRevertData(error)
   if (data) {
     try {
-      const decoded = decodeErrorResult({ abi: Abis.abis, data })
+      const decoded = decodeErrorResult({ abi: abis, data })
       const template = messages[decoded.errorName as AbiErrorName]
       return {
         ...decoded,
