---
"@wagmi/core": minor
"wagmi": minor
---

Updated Tempo APIs for viem 2.54.0: token balance and allowance reads now return `Amount` objects, and token `.call` helpers now accept the client before their action parameters.
